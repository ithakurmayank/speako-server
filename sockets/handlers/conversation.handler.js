/**
 * TODO (next step): port ChatHub.JoinConversation / LeaveConversation,
 * gated by hubAuthorization.canAccessConversation(userId, conversationId).
 * Same ack-callback pattern as channel.handler.js.
 */
const registerConversationHandlers = (socket) => {
  // placeholder — filled in next step
};

export { registerConversationHandlers };
